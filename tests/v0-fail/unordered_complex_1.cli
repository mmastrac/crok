#!/usr/bin/env crok --v0

$ printf "a,b,c,u1,u2bad,u3,repeat" | tr ',' '\n'
unordered {
    sequence {
        ! a
        ! b
        ! c
    }
    unordered {
        ! u1
        ! u2
        ! u3
    }
    repeat {
        ! repeat
    }
}
